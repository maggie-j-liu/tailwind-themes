{
  "name": "tailwind-themes",
  "version": "1.0.0",
  "description": "A Tailwind CSS plugin that adds variants for multiple color themes.",
  "main": "index.js",
  "scripts": {
    "test": "jest"
  },
  "dependencies": {
    "postcss-selector-parser": "^6.0.6"
  },
  "peerDependencies": {
    "tailwindcss": ">=2.0.0"
  },
  "devDependencies": {
    "jest": "^27.0.6",
    "jest-matcher-css": "^1.1.0",
    "postcss": "^8.3.5"
  }
}
